var arr1 = [];
var arr2 = [15, 78, 24, 89, 23];

//check the first variable (arr1) type and empty 
if(Array.isArray(arr1) == true && arr1.length <= 0)
   console.log("arr1 is an array and it is empty <br><br>");
else 
      console.log("Either arr1 is not an array or it is not empty <br><br>");
   
//check the second variable (arr2) type and empty 
if(Array.isArray(arr2) == true && arr2.length <= 0)
   console.log("arr2 is an array and it is empty <br>");
else 
      console.log("Either arr2 is not an array or it is not empty </br>");
 
//The class can be expressed without assigning any name to it.
var emp = class {  
    constructor(id, name) {  
      this.id = id;  
      this.name = name;  
    }  
  };  
  console.log(emp.name);  

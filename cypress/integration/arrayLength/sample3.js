var arr = new Array();  
arr['a'] = 100;  
arr['b'] = 200;  
arr['c'] = 300;  
arr['d'] = 400;  
arr['e'] = 500;  
  
console.log("The length of array is: " + arr.length);  

console.log(arr['c']);
//In this example, the index of the array is non-numeric.
// Here, the array contains five elements with the non-numeric index. 
//We are applying the length property on the given array to see the effect.
// Now let's see how the array.length property works on the non-numeric index of the array.